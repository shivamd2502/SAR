# ============================================================
# EOS-04 SAR ML/DL PIPELINE — REQUIREMENTS
# ============================================================
# Install all dependencies with:
#   pip install -r requirements.txt
#
# If geopandas fails on Windows, use:
#   pip install geopandas --only-binary :all:
#
# If rasterio fails on Windows, use:
#   pip install rasterio --find-links https://girder.github.io/large_image_wheels
#
# Tested on: Python 3.10 / 3.11, Windows 11, CUDA 12.1
# GPU: NVIDIA GeForce RTX 3050 Laptop GPU
# ============================================================

# ── Core scientific stack ──────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# ── Geospatial / SAR data reading ─────────────────────────
rasterio>=1.3.0          # reads GeoTIFF files with GPS metadata
geopandas>=0.14.0        # creates shapefiles for QGIS
shapely>=2.0.0           # geometry (polygon building for shapefiles)
fiona>=1.9.0             # shapefile I/O backend for geopandas
pyproj>=3.5.0            # CRS / coordinate reprojection

# ── Image processing ──────────────────────────────────────
Pillow>=10.0.0           # PNG/JPEG read-write for patches

# ── Deep learning ─────────────────────────────────────────
torch>=2.1.0             # PyTorch core
torchvision>=0.16.0      # pretrained models (EfficientNet-B0, ResNet34)
segmentation-models-pytorch>=0.3.3  # U-Net with pretrained encoders

# ── Visualization ─────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Machine learning utilities ────────────────────────────
scikit-learn>=1.3.0      # train_test_split, metrics, confusion matrix

# ── Label Studio mask conversion ──────────────────────────
label-studio-sdk>=0.0.32 # official RLE brush mask decoder

# ── Excel / CSV label loading ─────────────────────────────
openpyxl>=3.1.0          # reads .xlsx files if needed

# ── Hugging Face (needed by segmentation-models-pytorch) ──
huggingface-hub>=0.19.0

# ── Optional but recommended ──────────────────────────────
# tqdm>=4.65.0           # progress bars (nice to have for long loops)
# ipykernel>=6.25.0      # if using Jupyter notebooks
